# Backend API base URL used by configurationApi.ts
# With the Vite proxy configured in vite.config.ts, you can leave this blank
# and requests to /api will be proxied automatically.
# Set this only if you want to bypass the proxy and hit the backend directly.
VITE_API_BASE_URL=
